export default function App() {
  const count = 0;

  // JSX => Javascript + XML
  return (
    <div>
      <div>Lek CodeMobiles {count}</div>
      <div>Lek CodeMobiles {count}</div>
      <div>Lek CodeMobiles {count}</div>
    </div>
  );
}
